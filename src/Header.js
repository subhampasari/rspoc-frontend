import React from 'react';

function Header() {
    var style = {
        padding: '3rem',

    };
    return (
        <div style={style}>
            Experiences, Products and Features
        </div>
    );
}

export default Header;