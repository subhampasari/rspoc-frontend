import React from 'react';

// Page header
function Header() {
    var style = {
        padding: '1rem',
    };
    return (
        <div>
            <div style={style}>
                Self Service for tagging ref-markers
            </div>
        </div>
    );
}

export default Header;